<?php
	// Definition des gains de points
	defined('GAIN_DAILY') || define('GAIN_DAILY', 20);
	defined('GAIN_UPLOAD') || define('GAIN_UPLOAD', 20);
	defined('GAIN_SUBSCRIBE') || define('GAIN_SUBSCRIBE', 40);

	// Definition des différents coûts des actions
	defined('COST_FLASH') || define('COST_FLASH', 20);
	defined('COST_VOTE') || define('COST_VOTE', 10);
	defined('COST_COMMENT') || define('COST_COMMENT', 5);
	
	// Definition des values des votes
	defined('VALUE_VOTE_PROFIL') || define('VALUE_VOTE_PROFIL', 5);
	